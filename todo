removing folder you are in does not work perfectly. can not change to absolute
path after removing the folder I am in
pwd does not recognize "-" options.
if pwd -hi is entered it should through an error and not print.

closing heredoc with ctrl + d results in fd errors

at the moment we do not support ambiguous redirection.
meanin when export a="a b" and then echo >  it should print ambigu....

but we write "b" into file named a
