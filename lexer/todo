



* create lexer
	- a function that converts a string into tokens (words)

* create parser
	- assign meaning to the tokens
		+ what is a command?
		+ what is an argument
		+ ...


possible strings:

* [command] [argument][<<][file] 

tokens that don't need to be delimited by space on both sides
	
	* | < > << >> 

tokens that don't need to be delimited by space on on side

	* "
 


